---
- name: Launch proxy container
  community.docker.docker_container:
    name: apache
    image: romainnn/devops_proxy:latest
    ports:
      - "80:80"
    networks:
      - name: proxy-network
    pull: true
    recreate: true
    state: started
    restart_policy: always
